# MERMaid 项目综合大纲

> 基于仓库文件结构、`README.md` 原始大纲以及 `command.md` 实际运行记录整理。
>
> **实际使用的视觉/语言模型：OpenAI GPT‑5.2**（通过上海科技大学内部 API 代理）。

---

## 1. 项目简介

MERMaid (Multimodal aid for Reaction Mining) 是一个端到端的化学知识自动提取流水线，能够将学术 PDF 论文中的图、表格、反应式等异构信息自动转化为结构化的**知识图谱**。

三个核心模块如下：

| 模块 | 功能 |
|------|------|
| **VisualHeist** | 从 PDF 中分割并导出所有图像（表格、反应图等） |
| **DataRaider** | 多模态分析图像，提取化学反应的结构化数据（反应物、产物、条件、收率等） |
| **KGWizard** | 将结构化 JSON 翻译为图数据库（JanusGraph）中的知识图谱 |

你既可以单独使用任一模块，也可以通过 `run_mermaid.py` 或命令行 `mermaid RUN` 一键运行全流程。

---

## 2. 项目目录结构

```
MERMaid/
├── Assets/                          # 静态资源（样例 JSON 等）
├── PDFs/                            # 待处理的输入 PDF 文档
├── Prompts/                         # LLM 提示词模板
│   ├── base_prompt.txt              # 从图像提取化学反应的核心提示词
│   ├── check_image_prompt.txt       # 校验/完善已提取信息
│   ├── filter_image_prompt.txt      # 二分类提示词：判断图像是否包含化学反应
│   ├── inbuilt_keyvaluepairs.txt    # 预定义的反应参数键值列表
│   ├── update_dict_prompt.txt       # 更新/合并反应字典
│   └── archive/                     # 历史版本（针对电化学、有机、光催化等特定领域）
├── Results/                         # 所有输出内容
│   ├── Images/
│   │   ├── relevant_images/         # 被判定包含反应的图像
│   │   │   └── improperly_segmented_images/
│   │   └── irrelevant_images/       # 被过滤掉的无关图像
│   ├── JSON/                        # DataRaider 原始 JSON 输出
│   ├── JSONresults/                 # （备用）结果 JSON
│   ├── JSON_fixed/                  # 经过 fix_kgwizard_json.py 修复后的 JSON（用于 KGWizard）
│   ├── KGIntermediate/              # KGWizard 中间转换格式（供 parse 命令使用）
│   └── Graphs/                      # 最终知识图谱文件（.graphml）
├── scripts/
│   ├── startup.json                 # 全局配置文件（路径、模型、键值等）
│   └── fix_kgwizard_json.py         # 用户新增脚本：修复 DataRaider JSON 格式以适配 GPT‑5.2
├── src/
│   ├── visualheist/
│   │   ├── methods_visualheist.py   # 图像分割/提取核心算法（依赖 pdf2image、poppler 等）
│   │   └── __init__.py
│   ├── dataraider/
│   │   ├── process_images.py        # 主调度器：遍历图像，过滤→裁剪→提取→格式化→后处理
│   │   ├── filter_image.py          # 调用 VLM 过滤出含反应图像
│   │   ├── image_cropping.py        # 裁剪反应图区域（可能调用 VisualHeist 方法）
│   │   ├── api_access.py            # OpenAI API 调用封装（适配 max_completion_tokens）
│   │   ├── reaction_dictionary_formating.py  # 将模型输出解析为标准反应字典
│   │   ├── postprocess.py           # 后处理：清洗、校验、合并 JSON
│   │   ├── processor_info.py        # 常量、配置与模型参数定义
│   │   └── __init__.py
│   └── kgwizard/
│       ├── prompt/
│       │   ├── builder.py           # 组合 prompt 模板
│       │   ├── generator.py         # 调用 LLM 将反应字典转为图谱三元组
│       │   └── assets/              # header、instructions、tail 等模板片段
│       └── graphdb/
│           ├── schema_abstract.py   # 抽象图谱 Schema 基类
│           ├── janus.py             # 连接及操作 JanusGraph
│           └── schemas/
│               ├── echem.py         # 电化学反应 Schema
│               ├── org.py            # 有机合成 Schema
│               └── photo.py          # 光催化 Schema
├── webapp/
│   ├── fastapi_app.py               # 后端 REST API 服务
│   └── streamlit_app.py             # 浏览器端交互界面
├── run_mermaid.py                   # 总流水线入口（VisualHeist → DataRaider → KGWizard）
├── run_visualheist.py               # 单独运行 VisualHeist
├── run_dataraider.py                # 单独运行 DataRaider
├── run_kgwizard.py                  # 单独运行 KGWizard
├── launch_webapp.sh                 # 启动 Web 应用的 Shell 脚本
├── requirements.txt                 # 项目依赖
└── startup.json                     # 根目录下也可能存在的配置文件
```

---

## 3. 各模块文件功能与互相配合

### 3.1 VisualHeist – PDF 图像分割

**主要文件**：`src/visualheist/methods_visualheist.py`

- 使用 pdf2image/poppler 将 PDF 每页转为高分辨率图像。
- 通过布局分析、显著性检测等算法自动识别表格、反应图、普通插图区域，并导出为独立图片。
- 受配置 `model_size`（`base` 或 `large`）影响精度与内存占用。

**输入**：`PDFs/` 文件夹中的 PDF 文件。  
**输出**：`Results/Images/` 下所有提取出的图片（并不区分是否包含反应）。

---

### 3.2 DataRaider – 图像到结构化数据

| 文件 | 功能 |
|------|------|
| `process_images.py` | 主控逻辑，依次调用以下步骤。 |
| `filter_image.py` | 利用 GPT‑5.2 Vision 和 `filter_image_prompt.txt` 判断图片是否包含可提取的化学反应。若无，移入 `irrelevant_images`。 |
| `image_cropping.py` | 对有反应的图片进一步裁剪出单个反应区域（依赖 VisualHeist 的分割方法）。分割失败者放入 `improperly_segmented_images`。 |
| `api_access.py` | 封装对 OpenAI 兼容 API 的调用。已适配 GPT‑5.2 的 `max_completion_tokens`，并设置为通过上海科技大学代理（`genaiapi.shanghaitech.edu.cn`）请求。 |
| `reaction_dictionary_formating.py` | 解析 VLM 返回的文本，提取 reactants SMILES、products SMILES、solvent、temperature、yield 等标准字段，构造成 JSON。 |
| `postprocess.py` | 对原始 JSON 进行清洗：替换俗名为 IUPAC 名、统一键名、补全缺失字段。可通过修改其内部的 `COMMON_NAMES`、`KEYS` 字典进行自定义。 |
| `processor_info.py` | 存放全局常量、提示词路径、模型名称等配置。 |

**外部依赖**：RxnScribe（单独安装）用于光学化学结构识别，协助生成准确的 SMILES 字符串。

**处理流程**：

```
PDF 提取图像
    ↓
filter_image.py → GPT‑5.2 判定是否含反应
    ↓ (否) → irrelevant_images/
    ↓ (是)
image_cropping.py → 裁剪出单个反应区域
    ↓ (失败) → improperly_segmented_images/
    ↓
api_access.py → GPT‑5.2 Vision + base_prompt.txt → 提取反应信息
    ↓
reaction_dictionary_formating.py → 格式化为 JSON
    ↓
postprocess.py → 清洗 & 合并 → Results/JSON/
```

**输入**：`Results/Images/relevant_images/` 中的图像。  
**输出**：`Results/JSON/` 中每个 PDF 对应一个 `.json` 文件。

---

### 3.3 用户新增的 JSON 修复环节

**脚本**：`fix_kgwizard_json.py`（位于 `scripts/` 或根目录）

- 原因：GPT‑5.2 返回的 JSON 偶尔包含多余注释、嵌套混乱或不符合 KGWizard 的严格格式要求。
- 功能：批量化清洗 DataRaider 输出的 JSON，修复常见错误后保存至 `Results/JSON_fixed/`。
- 命令行示例：
  ```powershell
  python .\fix_kgwizard_json.py .\Results\JSON --output_dir .\Results\JSON_fixed
  ```

---

### 3.4 KGWizard – 结构化数据到知识图谱

| 文件 | 功能 |
|------|------|
| `prompt/generator.py` | 读取反应 JSON，按指定 Schema 构建提示词，调用 GPT‑5.2 生成图谱节点和边。 |
| `prompt/builder.py` | 负责动态拼接 prompt 模板（header + instructions + tail），支持关键词替换（`--substitutions`）。 |
| `graphdb/janus.py` | 连接本地 JanusGraph 服务器，执行图创建、清空、写入等操作。 |
| `graphdb/schemas/` | 提供三种预定义图谱 Schema：`echem`（电化学）、`org`（有机）、`photo`（光催化）。 |

**两个子命令**：

1. **`kgwizard transform`**  
   - 输入：`Results/JSON_fixed/`（或 `Results/JSON`）中的 JSON 文件。  
   - 输出：中间转换文件到 `Results/KGIntermediate/`；同时可选地直接将图谱写入 JanusGraph。  
   - 支持并行处理（动态调节 worker 数）、RAG 增强等。

2. **`kgwizard parse`**  
   - 输入：`Results/KGIntermediate/` 中的中间 JSON。  
   - 输出：JanusGraph 中的图谱 + 可选的 `.graphml` 导出文件。

**最终输出**：`Results/Graphs/` 下的 `.graphml` 文件，可直接导入 Gephi、Cytoscape 等工具进行可视化或分析。

---

## 4. 实际运行流程（Windows PowerShell）

依据 `command.md`，典型的运行步骤为：

1. **启动 JanusGraph 服务器**（需 Java 8）
   ```powershell
   cd janusgraph-1.1.0
   .\bin\gremlin-server.bat .\conf\gremlin-server\gremlin-server.yaml
   ```

2. **配置环境变量**
   ```powershell
   $env:OPENAI_API_KEY="..."              # 或通过 .env 文件设置
   $env:POPPLER_PATH="C:\Program Files\poppler-25.12.0\Library\bin"
   $env:Path="$env:POPPLER_PATH;$env:Path"
   $env:NO_PROXY="genaiapi.shanghaitech.edu.cn"
   $env:HF_ENDPOINT="https://hf-mirror.com"
   ```

3. **图像分割 – VisualHeist**
   ```powershell
   visualheist --config ./scripts/startup.json
   ```

4. **反应提取 – DataRaider**
   ```powershell
   dataraider --config ./scripts/startup.json *> .\dataraider_debug.log
   ```

5. **（可选）清空已有图谱**
   运行一段 Python Gremlin 脚本删除 `g` 图中所有节点与边。

6. **修复 JSON（GPT‑5.2 适配）**
   ```powershell
   python .\fix_kgwizard_json.py .\Results\JSON --output_dir .\Results\JSON_fixed
   ```

7. **构建知识图谱 – KGWizard**
   ```powershell
   kgwizard transform .\Results\JSON_fixed `
       --output_dir .\Results\KGIntermediate `
       --schema photo `
       --graph_name g `
       --address ws://localhost --port 8182 `
       --output_file "...\Results\Graphs\g.graphml"
   ```
   或分步执行：
   ```powershell
   kgwizard parse ./Results/KGIntermediate --schema photo --graph_name g --address ws://localhost --port 8182 --output_file "...\Graphs\g.graphml"
   ```

8. **验证图谱**
   使用 Gremlin 查询节点/边数量及样例数据。

---

## 5. 模型调用总结

| 阶段 | 模型/工具 | 提示词/配置 | 用途 |
|------|-----------|-------------|------|
| 图像过滤 | GPT‑5.2 (Vision) | `filter_image_prompt.txt` | 判断图像是否包含化学反应 |
| 反应提取 | GPT‑5.2 (Vision) + RxnScribe | `base_prompt.txt` / `check_image_prompt.txt` | 提取反应物、产物 SMILES、溶剂、温度、收率等 |
| 反应字典后处理 | 本地规则引擎 | `postprocess.py`（内部字典） | 清理、标准化 JSON |
| JSON 格式修复 | 自定义 Python 脚本 | `fix_kgwizard_json.py` | 修复 GPT‑5.2 输出格式问题 |
| 图谱生成 | GPT‑5.2 (Text) | `generator.py` + Schema 模板 | 将反应字典转为图谱三元组 |
| 图像分割 | 传统 CV + 布局算法 | `model_size` 配置 | 表格、图、反应区域检测与导出 |

**重要变更（对比原版）**：
- 使用 GPT‑5.2 代替原版 GPT‑4/4V，API 参数从 `max_tokens` 改为 `max_completion_tokens`。
- 通过内部代理服务器 `genaiapi.shanghaitech.edu.cn` 访问，需设置 `NO_PROXY`。
- 引入 `fix_kgwizard_json.py` 增强 JSON 兼容性。

---

## 6. 输入与输出完整清单

| 阶段 | 输入 | 输出 | 格式 |
|------|------|------|------|
| VisualHeist | `PDFs/` 中的 PDF | `Results/Images/` 下所有提取图像 | PNG/JPG |
| DataRaider | `Results/Images/relevant_images/` | `Results/JSON/*.json` | 反应字典 JSON |
| JSON 修复 | `Results/JSON/` | `Results/JSON_fixed/` | 标准化 JSON |
| KGWizard transform | `Results/JSON_fixed/` | `Results/KGIntermediate/` + JanusGraph 更新 | 中间 JSON |
| KGWizard parse | `Results/KGIntermediate/` | `Results/Graphs/*.graphml` + JanusGraph 最终图 | GraphML |
| Web App | `PDFs/` + 用户配置 | 同上述各阶段输出 | 同上 |

---

## 7. 与原 README 的关键差异

1. **模型升级**：文档原定使用 OpenAI GPT‑4 系列，实际部署采用 **GPT‑5.2**。
2. **API 代理**：所有请求通过 **上海科技大学内部 API 网关** 转发，而非直接调用 OpenAI 官方端点。
3. **新增修复脚本**：`fix_kgwizard_json.py` 弥补了 DataRaider 输出格式不稳定的问题。
4. **运行环境**：由原文档的 Linux/bash 转为 **Windows PowerShell**，并显式配置了 Poppler、Java 路径。
5. **额外测试**：增加了 `check_api_config.py` 用于 API 联通性诊断，以及通过 Gremlin 脚本手动清空 / 查询图数据库。

---

## 8. 如何扩展与自定义

- **支持新化学领域**：  
  在 `src/kgwizard/graphdb/schemas/` 中按模板添加新的 Schema 文件，并通过 `--schema` 参数指定。
- **调整提取参数**：  
  编辑 `Prompts/inbuilt_keyvaluepairs.txt` 和 `scripts/startup.json` 中的 `keys`、`new_keys` 列表。
- **后处理规则**：  
  修改 `dataraider/postprocess.py` 中的 `COMMON_NAMES` 和 `KEYS` 字典，以适应特定的溶剂、试剂俗名。
- **过滤逻辑**：  
  自定义 `Prompts/filter_image_prompt.txt` 可改变图像筛选标准。

---

*此大纲综合了项目仓库实际结构与 `command.md` 记录的运行流程，可作为开发与使用主要参考。*